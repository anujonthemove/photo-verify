[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "photo-verify"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["Pillow>=9.0", "pyyaml>=6.0"]

[project.scripts]
photo-verify = "run:main"
